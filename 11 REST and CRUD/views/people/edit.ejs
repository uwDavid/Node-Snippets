<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Person</title>
</head>

<body>
    <a href="/people">Back to List of all people</a>
    
    <h1><%= row.first_name %> <%= row.last_name %></h1>
    <div>Gender: <%= row.gender %></div>
    <div>Date of Birth: <%= row.date_of_birth %></div>
    <div>Country: <%= row.country_of_birth %></div>
    <div>e-mail: <%= row.email %></div>
    <hr>
    <form action="/people/<%= row.id %>?_method=PUT" method="POST">
        <div>
            <label for="first_name">First name:</label>
            <input type="text" name="first_name" value="<%= row.first_name %>">
        </div>
        <div>
            <label for="last_name">Last name:</label>
            <input type="text" name="last_name" value="<%= row.last_name %>">
        </div>
        <div>
            <label for="email">e-mail:</label>
            <input type="email" name="email" value="<%= row.email %>">
        </div>  
        <div>
            <label for="gender">Gender:</label>
            <input type="text" name="gender" value="<%= row.gender %>">
        </div>
        <div>
            <label for="date_of_birth">Date of Birth:</label>
            <input type="date" name="date_of_birth">
        </div>
        <div>
            <label for="country_of_birth">Country of Birth:</label>
            <input type="text" name="country_of_birth" value="<%= row.country_of_birth %>">
        </div>
        <button>Update Person Info</button>
        <!-- <input type="submit" value="Submit"> -->
    </form>
    <a href="/people">List of all people</a>

</body>

</html>


